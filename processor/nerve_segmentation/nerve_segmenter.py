import keras


class NerveSegmenter:
    def __init__(self, model_path, config) -> None:
        # self._model = keras.models.load_model(model_path)
        self._model = "TODO"  # TODO

    def segment(self, image):  # TODO: Multiple patches or just single one?
        # return self._model.predict(image) # TODO
        return []

    def train():
        pass

import keras


class TumorClassifier:
    def __init__(self, model_path) -> None:
        # self._model = keras.models.load_model(model_path)
        self._model = "TODO"  # TODO

    def classify(self, image):  # TODO: Multiple patches or just single one?
        # return self._model.predict(image) # TODO
        return true
